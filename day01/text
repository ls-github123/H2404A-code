# 调整类方法
 # capitalize()  字符串.capitalize()
# 将整个字符串中首字母调整为大写，其他字母全部调整为小写
# title() 
# upper() -- 字符串中所有字母调整为大写
# lower() -- 字符串中所有字母调整为小写

# 判别类方法
# startswith('字符')
# endswith('字符')
# isalpha() -- 判断字符串是否全由字母组成
# isalnum() -- 判断字符串是否全由字母或数字或字母和数字组成
# isdigit() -- 判断字符串是否全由数字组成
# isupper() -- 判断字符串中各字母是否全为大写
# islower() -- 判断字符串中各字母是否全为小写
# istitle() -- 判断字符串中各单词是否全为首字母大写其余字母全文小写
