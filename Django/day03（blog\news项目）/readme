1. 创建django项目，创建对应news子应用，根据新闻的增删改查完成对应一下任务：
1、根据描述，创建新闻模型类，至少5个字段
2、创建增删改查4个对应的几个视图。（视图函数即可，不用区分请求方式，在函数中完成操作）
3、通过访问新增视图，插入至少5条数据
4、通过访问修改视图，修改id为1的新闻的标题
5、通过访问删除视图，删除id为2的新闻数据
6、访问查询视图，查询新闻数据，以id为id降序查询
7、访问查询视图，查询阅读量大于100和点赞量大于50的新闻数据

2. 在Django项目中，创建一个名为Blog的子应用，该应用包含一个博客模型,用户模型。博客模型有title、content和published_date等至少三个字段。
用户模型有用户名、年龄身高等至少3个字段，请完成以下任务：
1、根据描述，完成项目创建，模型创建
2、模型请按关系正确完成对应表关系创建
3、创建添加数据视图，添加用户数据，和博客数据，至少2条用户数据，每个用户至少5条数据
4、创建一个视图函数，用于查询任意一个用户最近发布的5篇博客文章。
5、视图函数可以正确返回模板页面。
3、在模板中，遍历并显示每篇文章的标题、内容等数据。
4、扩展：在每个文章后面，添加一个删除按钮，可以是a标签，可以是按钮。点击完成调用删除视图。
